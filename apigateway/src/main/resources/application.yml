server:
  port: 8080

spring:
  cloud:
    gateway:
      server:
        webflux:
          routes:
          - id: Book-service
            uri: http://localhost:8081
            predicates:
              - Path=/api/books/**
            filters:
              - AddRequestHeader=X-Request-Source, API-GATEWAY